import React from "react";

const FooterBottom = () => {
  return (
    <div className="w-full overflow-x-clip border-t border-white/4">
      <p
        className="text-center font-normal uppercase tracking-[0.25em] text-white/4 select-none pointer-events-none py-4 sm:py-6"
        style={{ fontSize: "clamp(2rem, 7.5vw, 7.5rem)" }}
      >
        {/* arch INNER */}
      </p>
    </div>
  );
};

export default FooterBottom;
